// BFF API 路由配置文件
// 用于组织和管理API路由

// Modern.js BFF会自动根据文件路径映射路由
// 无需额外的路由定义文件

// API路由映射规则：
// - api/lambda/health.ts -> /api/health
// - api/lambda/chat/index.ts -> /api/chat
// - api/lambda/hello.ts -> /api/hello

// 导出空对象以避免模块错误
export default {};